# 字幕后端本地整理记录（2026-08-23）

本分支只整理本机已有的后端源码；不包含运行时 `data/.config.yaml`、API Key、服务器临时目录或部署产物。

## 字幕链路

- `main/xiaozhi-server/core/providers/asr/aliyunbl_stream.py`
  - 保持阿里百炼 Paraformer 流式识别连接，句间不重复握手。
  - `caption_mode` 下区分 `partial`（替换当前活动句）与 `append`（定稿句）。
  - 使用 `stream_id`、`segment_id`、`seq`、`revision` 防乱序和重复最终包。
  - 中文输出门槛阻断日文假名、韩文、俄文及无汉字结果。
  - ASR 异常向设备发送可见的恢复提示。

- `main/xiaozhi-server/core/utils/caption.py`
  - 提供中文过滤、句末标点、活动句长度限制、段落去重和单调序列元数据。

- `main/xiaozhi-server/core/handle/sendAudioHandle.py`
  - 保持旧版 `stt.text` 字段，同时允许附加 caption 元数据，兼容新旧固件。

- `main/xiaozhi-server/core/handle/receiveAudioHandle.py`
  - 字幕模式静音超时直接结束字幕连接，不进入 LLM/TTS 对话链路。

## 配置模板

- `main/xiaozhi-server/config.yaml`
  - `caption_mode: true`
  - `enable_websocket_ping: true`
  - `close_connection_no_voice_time: 3600`
  - `semantic_punctuation_enabled: false`
  - `max_sentence_silence: 900`
  - `multi_threshold_mode_enabled: true`
  - `language_hints: ["zh"]`
- `main/xiaozhi-server/data/.config.yaml.example`
  - 仅提供占位字段，运行时密钥不入库。

## 测试与工具

- `main/xiaozhi-server/tests/test_caption_protocol.py`：字幕元数据、中文过滤、去重和兼容字段测试。
- `main/xiaozhi-server/tools/caption_preflight.py`：启动前检查配置、依赖和协议前置条件。
- `main/xiaozhi-server/.github/workflows/caption-tests.yml`：在提交时运行字幕协议测试。

## 日志说明

本机整理目录中没有可核验的后端运行时 `server.log` 或识别日志文件，因此没有伪造或凭空补写日志内容。运行日志应留在部署实例的 `tmp/` 下，并通过脱敏后的摘录另行归档；日志中的密钥、设备 MAC 和音频内容不得提交。
