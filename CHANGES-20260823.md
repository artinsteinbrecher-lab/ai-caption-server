# 2026-08-23 现网服务器改动

- `main/xiaozhi-server/core/providers/asr/aliyunbl_stream.py`：将 ASR 改为长连接；最终句后 `continue`，不再每句断开；增加 `idle_timeout_sec=30` 静音守卫，空闲后结束任务并在下次语音时自动重连。
- `main/xiaozhi-server/core/providers/asr/aliyunbl_stream.py`：发送字幕标记协议，流式中间结果使用 `partial`，最终句使用 `append`，并附带 `stream_id` 与递增 `seq`，用于固件端替换活动行、防乱序和防重复。
- `main/xiaozhi-server/core/handle/sendAudioHandle.py`：`send_display_message` 支持可选 `caption` 字段，未传入时保持原协议行为。
- `main/xiaozhi-server/core/handle/receiveAudioHandle.py`：`caption_mode` 下静音超时直接关闭连接，避免进入普通 LLM 对话流程。
- `main/xiaozhi-server/data/.config.yaml`（不纳入 Git）：现网使用 `semantic_punctuation_enabled=false`、`max_sentence_silence=900`、`close_connection_no_voice_time=3600`、`enable_websocket_ping=true`、`caption_mode=true`。公开模板见 `main/xiaozhi-server/config.example.yaml`。
- 配套固件协议：`artinsteinbrecher-lab/ai-caption-firmware` PR #2。
